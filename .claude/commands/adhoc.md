Handle an ad-hoc request outside current sprint.

Request: $ARGUMENTS

1. Label as `[AD-HOC]`
2. Create isolated task in `.prism/adhoc/ADHOC_NNN.md`
3. Execute the request
4. After completion, extract reusable knowledge into `.prism/knowledge/`
5. Do NOT modify `MASTER_PLAN.md` — this is outside the sprint
