Design a production agent pipeline (Brain → Body pattern).

What to automate: $ARGUMENTS

1. Identify agents needed (Fetch, Detect, Post, Coordinator, Analysis)
2. Assign model tier per agent (Tier 1-4) to minimize cost
3. Design data flow between agents
4. Define fail behavior and retry logic per agent
5. Estimate daily/monthly cost
6. Create standalone scripts ready for deployment
7. Save pipeline design to `.prism/designs/pipeline_{name}_{date}.md`
