Headless browser. Navigate, click, screenshot.

URL or action: $ARGUMENTS

Route to gstack cognitive mode via gstack-bridge.
→ Lazy-load `gstack/browse/SKILL.md`

Browser session persists between calls (cookies, tabs).
